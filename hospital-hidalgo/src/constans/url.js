/* url de la base de la api */

const url_base = 'http://localhost:4000/api/';

const url = {
    'patients': url_base + 'patients/',
    'users': url_base + 'users/',
    'userRoles': url_base +'userRoles/',
};

export default url;